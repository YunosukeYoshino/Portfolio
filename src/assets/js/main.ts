import { setUpAccordion, Hamburger } from "./Common";
import { TestAnimation } from "./Gsap";

document.addEventListener("DOMContentLoaded", () => {
  TestAnimation(); //Gsap Test Animation

  setUpAccordion(); //Accordion
  Hamburger(); //Hamburger Menu
});
