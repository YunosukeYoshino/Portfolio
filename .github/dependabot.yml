version: 2
updates:
  # Bun/npm dependencies are refreshed weekly by jules-dependency-update.yml,
  # so Dependabot here only covers GitHub Actions versions.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    commit-message:
      prefix: chore
    labels:
      - dependencies
